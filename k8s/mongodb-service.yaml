apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: resume-screening
  labels:
    app: mongodb
    component: database
spec:
  type: ClusterIP  # Internal only - no external access
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb


